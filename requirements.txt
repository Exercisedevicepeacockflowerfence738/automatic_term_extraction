# The core pipeline (ate_from_scratch.py) needs NOTHING from this file.
# These are only required to regenerate the figures and rebuild the notebook.

matplotlib>=3.5      # figures/generate_figures.py, figures/make_cover.py
numpy>=1.21          # figures/generate_figures.py
pandas>=1.3          # notebook display tables

# Optional — only for rebuilding and executing the notebook:
# nbformat>=5.0
# nbclient>=0.7
